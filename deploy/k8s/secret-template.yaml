apiVersion: v1
kind: Secret
metadata:
  name: compliance-mapping-service-secrets
type: Opaque
stringData:
  # Template only. Do not commit real secret values.
  SPRING_DATASOURCE_URL: "REPLACE_WITH_DATABASE_URL"
  SPRING_DATASOURCE_USERNAME: "REPLACE_WITH_DATABASE_USERNAME"
  SPRING_DATASOURCE_PASSWORD: "REPLACE_WITH_DATABASE_PASSWORD"
  APP_STORAGE_S3_BUCKET: "REPLACE_WITH_BUCKET_NAME"
